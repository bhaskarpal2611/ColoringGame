using UnityEngine;

/// <summary>Minimal stub so TGM_AudioManager compiles; expand if per-line data is needed later.</summary>
[CreateAssetMenu(fileName = "VoiceLineData", menuName = "Audio/Voice Line Data")]
public class VoiceLineData : ScriptableObject
{
    public string clipName;
}
