namespace ColorSwipeGame
{
    // used for indexing custom brush material array
    public enum PaintMode
    {
        Color, Erase, Texture,
    }

    public enum GameMode
    {
        None, Color, Draw
    }
}